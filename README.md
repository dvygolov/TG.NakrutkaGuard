# 🛡 Nakrutka Guard Bot

Telegram-бот для защиты групп и каналов от накрутки подписчиков/участников.

## Особенности

- ⚡ **Высокая производительность** - асинхронная обработка через aiogram 3.x
- 🎯 **Умная детекция** - скользящее окно для анализа вступлений
- 🔄 **Автоматическая защита** - включается при превышении порога, отключается при нормализации
- 👑 **Защита Premium** - опциональная защита Telegram Premium пользователей
- 📊 **Детальное логирование** - файловые логи по чатам и датам
- ⚙️ **Гибкая настройка** - управление через inline-клавиатуру

## Как работает

1. **Обычный режим**: Бот считает вступления в скользящем окне времени
2. **Детекция атаки**: При превышении порога (например, 10 вступлений/мин) активируется защита
3. **Режим защиты**: Все вступающие кикаются автоматически (кроме Premium если настроено)
4. **Автоотключение**: Когда поток вступлений возвращается в норму, защита отключается

## Установка (Linux)

### 1. Клонировать репозиторий
```bash
git clone <repo_url>
cd TG.NakrutkaGuard
```

### 2. Собрать окружение
```bash
chmod +x build.sh
./build.sh
```

Скрипт создаст виртуальное окружение `.venv` и установит зависимости.

### 3. Настроить .env
```bash
cp .env.example .env
nano .env  # или vim/любой редактор
```

Отредактировать `.env`:
```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=123456789,987654321
```

### 4. Запустить бота

**Вручную (foreground):**
```bash
.venv/bin/python -m bot.main
```

**В фоне через nohup:**
```bash
chmod +x start.sh
./start.sh
```

**Остановить:**
```bash
chmod +x stop.sh
./stop.sh
```

**Как systemd service (автозапуск при перезагрузке):**
```bash
chmod +x install-service.sh
sudo ./install-service.sh
```

Управление службой:
```bash
# Статус
sudo systemctl status nakrutkaguard

# Остановить
sudo systemctl stop nakrutkaguard

# Запустить
sudo systemctl start nakrutkaguard

# Перезапустить
sudo systemctl restart nakrutkaguard

# Логи
tail -f nakrutkaguard.service.log
```

**Удалить службу:**
```bash
sudo ./install-service.sh -u
```

## Настройка бота

### Получение токена
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен в `.env`

### Добавление админов
В `.env` укажите ID админов через запятую:
```env
ADMIN_IDS=123456789,987654321
```

Узнать свой ID: [@userinfobot](https://t.me/userinfobot)

## Использование

### 1. Добавить чат под защиту
- Отправьте боту `/start`
- Нажмите "➕ Добавить чат"
- Перешлите сообщение из нужного чата или отправьте его ID

### 2. Добавить бота в чат/канал
⚠️ **Важно**: Бот должен быть **администратором** с правами:
- ✅ Удаление пользователей (Ban users)
- ✅ Приглашение пользователей (Invite users)

### 3. Настроить параметры
- **Порог** - количество вступлений для активации защиты (по умолчанию: 10)
- **Временное окно** - период в секундах для подсчёта (по умолчанию: 60)
- **Premium защита** - не кикать Telegram Premium пользователей (по умолчанию: Да)

## Структура проекта

```
TG.NakrutkaGuard/
├── bot/
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── database.py          # Работа с БД (aiosqlite)
│   ├── handlers/
│   │   ├── setup.py         # Настройка чатов (inline-клавиатура)
│   │   └── members.py       # Обработка новых участников
│   └── utils/
│       ├── logger.py        # Файловое логирование
│       └── detector.py      # Детекция атак
├── data/
│   └── bot.db              # SQLite база
├── logs/
│   ├── my_channel/         # Логи по чатам (@username)
│   │   └── 2025-12-08.log
│   └── chat_1001234567/    # Или по ID если нет username
│       └── 2025-12-08.log
├── requirements.txt
├── .env
└── README.md
```

## База данных

### chats
Чаты под защитой с настройками

### join_events
Лог всех вступлений для подсчёта в скользящем окне

### attack_sessions
История атак со статистикой

## Логи

Логи сохраняются в `logs/[username или chat_id]/YYYY-MM-DD.log`

Формат:
```
[2025-12-08 14:30:15] INFO: JOIN | USER | ID: 123456789 | Username: john_doe
[2025-12-08 14:30:45] WARNING: ATTACK STARTED | Threshold: 10 | Detected: 12 joins
[2025-12-08 14:30:46] INFO: KICK | ID: 987654321 | Username: bot_user | Reason: protection
[2025-12-08 14:35:20] WARNING: ATTACK ENDED | Duration: 4m 35s | Total kicked: 87
```

## Уведомления админам

### Начало атаки
```
⚠️ АТАКА ОБНАРУЖЕНА

📊 Порог: 10 вступлений/60с
🔴 Обнаружено: 12 вступлений
🛡 Режим защиты: АКТИВЕН
```

### Конец атаки
```
✅ АТАКА ЗАВЕРШЕНА

⏱ Длительность: 4м 35с
🚫 Кикнуто: 87
```

## Технологии

- **aiogram 3.15.0** - async Telegram Bot API framework
- **aiosqlite 0.20.0** - async SQLite driver
- **python-dotenv 1.0.1** - управление .env

## Требования

- Python 3.9+
- Telegram Bot API Token

## Troubleshooting

### Бот не кикает пользователей
- Проверьте что бот админ с правами на удаление
- Проверьте логи на наличие ошибок API

### Защита не активируется
- Проверьте настройки порога и окна
- Убедитесь что чат добавлен в систему защиты

### Rate limit ошибки
- Уменьшите порог срабатывания
- Бот автоматически батчит кики по 50 штук с задержкой

## Лицензия

MIT
