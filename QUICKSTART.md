# 🚀 Быстрый старт на Linux сервере

## Шаг 1: Подготовка
```bash
# Клонируем репозиторий
git clone <repo_url>
cd TG.NakrutkaGuard

# Даём права на выполнение
chmod +x build.sh start.sh stop.sh install-service.sh
```

## Шаг 2: Установка
```bash
# Собираем окружение
./build.sh

# Настраиваем .env
cp .env.example .env
nano .env
```

В `.env` укажи:
```env
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
ADMIN_IDS=123456789
```

## Шаг 3: Запуск

### Вариант A: Ручной запуск (для тестирования)
```bash
# Запустить
.venv/bin/python -m bot.main

# Или в фоне
./start.sh

# Остановить
./stop.sh
```

### Вариант B: Systemd service (рекомендуется)
```bash
# Установить как службу
sudo ./install-service.sh

# Проверить статус
sudo systemctl status nakrutkaguard

# Смотреть логи в реальном времени
tail -f nakrutkaguard.service.log
```

## Управление службой

```bash
# Остановить
sudo systemctl stop nakrutkaguard

# Запустить
sudo systemctl start nakrutkaguard

# Перезапустить (после обновления кода)
sudo systemctl restart nakrutkaguard

# Удалить службу
sudo ./install-service.sh -u
```

## Использование бота

1. Найди бота в Telegram и отправь `/start`
2. Нажми "➕ Добавить чат"
3. Перешли сообщение из нужного чата/канала
4. Добавь бота **админом** в этот чат/канал с правами:
   - ✅ Ban users
   - ✅ Invite users
5. Настрой параметры через inline-клавиатуру

## Логи

**Логи службы:**
```bash
tail -f nakrutkaguard.service.log
```

**Логи по чатам:**
```bash
ls logs/                    # Список чатов
tail -f logs/@channel_name/2025-12-08.log
```

## Обновление бота

```bash
# Остановить
sudo systemctl stop nakrutkaguard

# Обновить код
git pull

# Обновить зависимости (если изменились)
./build.sh

# Запустить
sudo systemctl start nakrutkaguard
```

## Проблемы?

**Бот не кикает:**
- Проверь что бот админ с правами на ban
- Проверь логи: `tail -f nakrutkaguard.service.log`

**Служба не запускается:**
```bash
sudo systemctl status nakrutkaguard
journalctl -u nakrutkaguard -n 50
```

**Проверить что бот работает:**
```bash
ps aux | grep "bot.main"
```
